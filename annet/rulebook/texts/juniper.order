
forwarding-options
    port-mirroring
        ~
    activate port-mirroring
        ~


# A rule for all the commands
* %global
# Weights are assigned to the comments so that they are always applied after the commands, but only in the patch
*/\/\*(?:(?!\*\/).)*\*\// %global %scope=patch
