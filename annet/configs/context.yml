generators:
  default:
    - annet_generators.example
    - annet_generators.mesh_example
    - annet_generators.rpl_example

storage:
  default:
    adapter: netbox

rulebooks:
  default:
    path: annet.rulebook.texts

context:
  default:
    generators: default
    storage: default
    rulebooks: default

selected_context: default
