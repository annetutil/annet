- name: flat vlan list with named single-id block
  vendor: asr
  diff: |
    - vlan 1,1000-1001
    + vlan 1,1000-1002
    vlan 1000
      name foo
    + vlan 1002
      + name bar
  patch: |
    configure exclusive
    vlan 1002
      name bar
      exit
    commit show-error
    exit
